name: voltrix
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Pacotes:
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2
  flutter_feather_icons: ^2.0.0 
  feather_icons: ^1.2.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Exemplo de onde colocar assets no futuro:
  # assets:
  #   - images/
